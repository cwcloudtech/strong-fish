# AI instruct 3

## UX/UI sf-ui

I want the dropdown to be design as in uprodit.com (like the language dropdown) in `~/uprodit`.

I want the logo bigger in the sidebar without "stong-fish" text.

I want the login/signup form to be on the right with a bigger FA competition's background (on free pictures) on the left as background.

I also want an `about` page which will be sourced from an `about.md` markdown file (prepare this file, I'll fill it later).

I also want a RGPD cookie's banner like in `~/cwclock` with the same text.
