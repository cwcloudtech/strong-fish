# AI instruct 18

On mobile I want also the share button which will open the app's suggestions directly for posts and profile.

On web, remove all social media that doesn't have a web share feature (it's redundant with copy).
