# À propos de strong-fish

<!--
Cette page vous appartient : l'application affiche le Markdown contenu dans ce
fichier, que vous pouvez donc modifier sans recompiler ni redéployer le
frontend.

La version anglaise se trouve dans `about.md`, à côté de ce fichier. La page
charge d'abord `about.<locale>.md` et retombe sur `about.md` lorsqu'il n'y a
pas de traduction.

Les titres, listes, liens, tableaux, images, blocs de code et citations sont
tous pris en charge. Supprimez ce commentaire lorsque vous remplirez la page.
-->

_Texte à rédiger._
